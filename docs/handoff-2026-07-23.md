# Session handoff — 2026-07-23

Context for a Claude Code session on the VPS (or the operator). Written from the
operator's Mac dev copy. Point-in-time notes — verify against live state before acting.

## What happened this session

1. **Live incident:** ForexEABot was looping on
   `MT5 initialize failed: (-10005, 'IPC timeout')`. Root cause: a wedged
   `terminal64.exe` holding the IPC pipe. `Restart-Service` alone does not clear it —
   the fix is `taskkill /F /IM terminal64.exe` then restart the bot so it relaunches a
   clean terminal. (The watchdog is *supposed* to do this automatically; see open items.)

2. **Decision — repoint the bot to Deriv, single market.** Trade ONLY
   `Volatility 10 (1s) Index` on **Deriv's MT5** platform (not Deriv's native API),
   one position at a time. "Commit its whole life to one trade" = single-symbol focus,
   NOT martingale/never-lose. Chosen scope: config-only, demo-first.

3. **Changes committed** (branch `hardening-and-docker`, pushed to
   `github.com/MartinDev69/Forex-EA`):
   - `.env.example` — Deriv MT5 defaults (`Deriv-Demo`), `SYMBOLS=Volatility 10 (1s) Index`,
     `MAX_OPEN_TRADES=1`, `CORRELATION_ENABLED=0`.
   - `deploy/README.md` — "Running Deriv — Volatility 10 (1s) only" runbook + IPC-timeout
     and missing-symbol troubleshooting.
   - `CLAUDE.md` — auto-loaded operating guide.

## Current state / what is NOT done yet

- **The live switch is NOT active.** The commits only touch the template + docs. The
  real `C:\forex-ea\.env` still holds the old Exness config until the operator edits it.
  The bot keeps trading its previous config until then.
- To activate: edit `C:\forex-ea\.env` per `deploy/README.md`, start on `Deriv-Demo`,
  confirm signal → execution on `Volatility 10 (1s) Index`, then switch `MT5_SERVER` to
  the real `DerivSVG-Server-0X` and restart.

## Open items

1. **Watchdog didn't auto-recover the IPC timeout.** `scripts/watchdog.py` `_recycle_mt5`
   should have killed the wedged terminal. Verify the scheduled task is installed and
   running (`Get-ScheduledTask *watchdog*`), check its last run result, and confirm the
   `WATCHDOG_BROKER_DISCONNECT_S` threshold vs how long it was stuck.
2. **Confirm the exact V10 symbol string** in the Deriv MT5 terminal's Market Watch —
   `SYMBOLS` must match it character-for-character (right-click Market Watch → Show All).
3. **Watch first-trade lot sizes on demo** — a synthetic index's point value differs
   from forex, so `RISK_PER_TRADE=0.01` maps to a different lot than on Exness.
4. **Stale root `README.md`** still says "Week 1 / scaffolding" — cosmetic, low priority.

## Good first prompt on the VPS

"Read CLAUDE.md and docs/handoff-2026-07-23.md, tail the latest logs and check service
status, then tell me the bot's current state and whether the Deriv/V10 switch is live yet."
