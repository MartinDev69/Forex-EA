# Core data stack
pandas>=2.1.0
numpy>=1.26.0
pyarrow>=15.0.0  # parquet backend for historical bar storage
python-dotenv>=1.0.0

# MT5 (Windows only — optional on macOS/Linux)
MetaTrader5>=5.0.45; sys_platform == "win32"

# Technical analysis
ta>=0.11.0

# Backtesting
backtrader>=1.9.78

# Machine learning (added later)
scikit-learn>=1.4.0
xgboost>=2.0.0

# Visualization
matplotlib>=3.8.0
plotly>=5.18.0

# Scheduling & monitoring
schedule>=1.2.0
python-telegram-bot>=20.7

# HTTP / API (for mobile app backend later)
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0

# Auth (dashboard login + protected endpoints)
bcrypt>=4.0.0
PyJWT>=2.8.0
cryptography>=42.0.0  # Fernet-encrypts broker credentials at rest

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0
httpx>=0.27.0  # used by fastapi.testclient.TestClient
